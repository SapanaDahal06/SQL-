use XYZ;

CREATE TABLE ADDELISE (
    DEPARTMENT_ID INT PRIMARY KEY,
    DEPARTMENT VARCHAR(50)
);

INSERT INTO ADDELISE
VALUES
(101, 'IT'),
(102, 'HR'),
(103, 'Finance');

CREATE TABLE SAPANA (
    EMP_ID INT PRIMARY KEY,
    EMP_NAME VARCHAR(50),
    SALARY INT,
    DEPARTMENT_ID INT,

    FOREIGN KEY (DEPARTMENT_ID)
        REFERENCES ADDELISE(DEPARTMENT_ID)
);
INSERT INTO SAPANA
VALUES
(6, 'Aashim', 75000, 101),
(7, 'Anu', 55000, 102),
(8, 'Rohan', 90000, 103),
(9, 'Puja', 65000, 101),
(10, 'Bikash', 80000, 103);

SELECT * FROM ADDELISE;
SELECT * FROM SAPANA;